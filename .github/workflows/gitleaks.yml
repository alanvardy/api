name: gitleaks
permissions:
  contents: read

on:
  push:
    branches:
      - main         # only run on pushes to the default branch
  pull_request:      # PR branches are covered here, avoiding duplicate runs
  workflow_dispatch:
  schedule:
    - cron: "0 4 * * *"

concurrency:
  group: gitleaks-${{ github.ref }}   # one run per branch/ref
  cancel-in-progress: true            # cancel older runs on new pushes

jobs:
  scan:
    name: gitleaks
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Run gitleaks
        uses: gitleaks/gitleaks-action@v3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITLEAKS_LICENSE: ${{ secrets.GITLEAKS_LICENSE }}
          GITLEAKS_CONFIG: .github/workflows/gitleaks/gitleaks.toml
